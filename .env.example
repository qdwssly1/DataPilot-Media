# Copy to .env only in a local environment. Never commit real API keys.
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=your-model-name
LLM_TIMEOUT_SECONDS=60

# Required for the real WrenToolAdapter. WREN_PROFILE is optional.
WREN_PROJECT_PATH=
WREN_PROFILE=
